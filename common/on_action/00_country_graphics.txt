on_game_initialized = {
	events = {
		#dependency_colour.2 # Give all subjects overlord colour if appropriate.
	}
}

on_subject_defect = { #scope:future_overlord = country that stole vassal, scope:target_subject  = vassal being stolen
	effect = {
		#scope:target_subject = { # Give the subject the colour of its new overlord
		#	trigger_event = { id = dependency_colour.1 }
		#}
	}
}

on_ending_war = {
	#events = {
	#	dependency_colour.3 # Restore original colour
	#}
}