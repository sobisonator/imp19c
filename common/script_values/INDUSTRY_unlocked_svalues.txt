# Svalues for storing whether an industry type can be built by the player
# Value = 1 if true
# Scope: player

INDUSTRY_unlocked_clothing = {
	value = 0
	if = {
		limit = {
			civic_tech >= 0 # Placeholder. Maybe reflect in the industrial_goods_buttons scripted GUI so it shows up in tooltip?
			# invention = # Should probably be linked to inventions instead!
		}
		value = 1
	}
}