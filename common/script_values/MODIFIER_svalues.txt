# Also contains ADDITIVE_ svalues which are pure additives rather than multipliers

MODIFIER_national_admin_points_consumed = {
	# Scope: Country
	value = 1
	add = modifier:global_export_commerce_modifier
}

ADDITIVE_national_admin_points_consumed = {
	# Scope: Country
	value = modifier:global_building_slot
}

MODIFIER_state_admin_points_consumed = {
	# Scope: State
	value = modifier:state_commerce_modifier
}

MODIFIER_country_all_state_admin_points_consumed = {
	# Scope: Country
	value = modifier:global_tax_modifier
}