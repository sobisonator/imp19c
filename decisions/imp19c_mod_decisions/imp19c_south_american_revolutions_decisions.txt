country_decisions = {
	invite_bolivar = {
		potential = {
			always = no # WiP
			# Mission tree reward?
			char:59 = { # Bolivar
				is_alive = yes
			}
			any_country = {
				war_with = ROOT
				OR = {
					tag = SPA
					tag = CHA
					trigger_if = {
						limit = {
							is_subject = yes
						}
						OR = {
							overlord = c:SPA
							overlord = c:NSP
						}
					}
				}
			}
		}
		allow = {
			war = yes
			government = revolutionary_republic
			political_influence > 50
			NOT = {
				has_variable = invited_bolivar
			}
			char:59 = {
				NOT = {
					has_variable = bolivar_invited_recently
					employer = ROOT
				}
			}
		}
		effect = {
			set_variable = invited_bolivar
			add_political_influence = -50
			char:59 = {
				set_variable = {
					name = bolivar_invited_recently
					days = 1095 # 3 Years
				}
			}
		}
	}

	form_argentina = {
		potential = {
			OR = {
				tag = LFP
				tag = AR1
			}
		}
		allow = {
			NOT = {has_variable = united_argentina}
			war = no
			AND = {
				owns_or_subject_owns = 3699 #Misiones
				owns_or_subject_owns = 6818 #Salta
				owns_or_subject_owns = 8996 #Tucuman
				owns_or_subject_owns = 3432 #Santa Fe
				owns_or_subject_owns = 966 #Cordoba
				owns_or_subject_owns = 1574 #San Juan
				owns_or_subject_owns = 7088 #Buenos Aires
				owns_or_subject_owns = 5059 #Mendoza
				owns_or_subject_owns = 6444 #Pellegrini

			}
		OR = {
            AND = {
                this = c:AR1
                OR = {
                    NOT = { c:LFP = { exists = yes } }
                    c:LFP = { has_land = no }
                }
            }
         
            AND = {
                this = c:LFP
                OR = {
                    NOT = { c:AR1 = { exists = yes } }
                    c:AR1 = { has_land = no }
            	    }
            	}
        	}
    	}
	effect = {
			change_country_name = Argentina
			trigger_event = argentine_event.10
			set_variable = united_argentina
		
			
			
    
    setup_add_claim_to_region = {
		region = region:Uruguay
		claimant = AR1
	}
    setup_add_claim_to_region = {
		region = region:Patagonia
		claimant = AR1
	}
    setup_add_claim_to_region = {
		region = region:Falkland_Islands
		claimant = AR1
	}
			add_political_influence = 100
			add_stability = 10
			add_aggressive_expansion = 15
		}
	}
	
}