namespace = resource_supply_demand_calc

# RESOURCE DEMAND CALCULATION EVENTS

# 1 = Food
resource_supply_demand_calc.1 = {
	type = country_event
	hidden = yes

	trigger = {

	}

	immediate = {

		every_in_global_list = {
			list = food_resource_category
			limit = { always = yes }
			change_global_variable = {
				name = THIS
				add = 123
			}
		}

	}
}