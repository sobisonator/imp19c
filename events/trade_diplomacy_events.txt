namespace = trade_diplomacy_event

trade_diplomacy_event.1 = { # Set up a trade deal for all categories of goods
	type = country_event
    title = subject_focus_individual.1.t
    desc = subject_focus_individual.1.desc
    picture = war_chest
    right_portrait = scope:recipient.current_ruler
    left_portrait = current_ruler

    trigger = {
    
    }

    immediate = {

    	

    }
}

# TODO: Move these events into AI_notification_events
trade_diplomacy_event.1 = {
    # Test event
    type = minor_country_event
    title = ai_notification_event.1.t
    desc = ai_notification_event.1.desc
    # picture = throneroom # TODO: Fix picture - not working ATM for some reason
    left_portrait = scope:trigger_country.current_ruler

    goto_location = scope:trigger_country.capital_scope

    interface_lock = no

    option = {
        name = ignore # Should be timeout default
    }
    option = {
        name = support
    }
    option = {
        name = condemn
    }
}