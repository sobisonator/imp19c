template fs {
	size = { 100% 100% }
}

template e2 {
	layoutpolicy_vertical = expanding
	layoutpolicy_horizontal = expanding
}

template ev { layoutpolicy_vertical = expanding }
template eh { layoutpolicy_horizontal = expanding }
